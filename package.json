{
  "name": "smhi-mcp",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SMHI MCP Server",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "echo \"No tests specified\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.0.0"
  }
}